@model Restaurant.Models.MeniuClient

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>MeniuClient</legend>

        @Html.HiddenFor(model => model.Id)

        <div class="editor-label">
            @Html.LabelFor(model => model.Meniu)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.Meniu)
            @Html.ValidationMessageFor(model => model.Meniu)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.Ingrediente)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.Ingrediente)
            @Html.ValidationMessageFor(model => model.Ingrediente)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
